export type Role = {
  id: string;
  title: string;
  description: string;
};

export const roles: Role[] = [];

export const lookingFor: string[] = [];

export const faqs: { q: string; a: string }[] = [];
